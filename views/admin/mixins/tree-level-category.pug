mixin tree-level-category(arr, level = 1, targetId = '')
    //- - const prefix = Array(level + 1).join('-- ')
    - let prefix = '';
    - for (let i = 0; i < level; ++i) {
        - prefix += '-- ';
    - }
        
    each item in arr 
        if (targetId)
            option(
                value=item.id 
                selected = (item.id == targetId ? true : false)
            ) #{prefix}#{item.title}
        else 
            option(
                value=item.id 
            ) #{prefix}#{item.title}

        if (item.children)
            +tree-level-category(item.children, level + 1, targetId)
        
